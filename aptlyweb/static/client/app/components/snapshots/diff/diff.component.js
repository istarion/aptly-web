import template from './diff.html';
import controller from './diff.controller';
import './diff.styl';

let diffComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default diffComponent;
